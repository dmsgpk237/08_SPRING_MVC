package com.ohgiraffers.example.member.model.entity;

public enum RoleType {
    USER, ADMIN
    // 이넘은 열거형 타입, 이넘이라고 만들어주고 멤버에다가 롤타입을 지정해줬다.
    // 멤버에 롤타입에 들어갈 수 있는 타입은 유저나 어드민 둘 중 하나만 들어갈 수 있다고
    // 지정 해준 것이다.
}
